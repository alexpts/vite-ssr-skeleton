{
  "name": "app2",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:browser && npm run build:ssr",
    "build:browser": "vite build --outDir ./dist/browser",
    "build:ssr": "vite build --outDir ./dist/ssr --ssr src/entry-ssr.js --manifest --ssrManifest",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.2.26",
    "vue-router": "^4.0.12"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^2.0.1",
    "sass": "^1.46.0",
    "vite": "^2.7.10"
  },
  "engines": {
    "node": "^17.3"
  }
}
